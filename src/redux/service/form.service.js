//Form handling

export const formSubmit = (signupData) => {
    /*return axios
        .post('https://gateway.tm.zurstiege.de/auth/create/', signupData)
        .then(async (response) => {
            return response.data;
        })
        .catch((e) => {
            console.log("error", e)
        })
     */
};